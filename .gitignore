# Per-node config (UUID, hostname). Each machine generates its own.
mousebridge.json

# Python
__pycache__/
*.py[cod]
*.egg-info/
*.egg
.venv/
venv/
.python-version

# IDE
.idea/
.vscode/

# OS
.DS_Store
Thumbs.db

# Old Rust artifacts (left over from before the Python rewrite)
target/
Cargo.lock
